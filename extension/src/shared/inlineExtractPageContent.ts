import { extractPageContent } from './extractPageContent'

/** @deprecated Use extractPageContent. Kept for existing executeScript call sites. */
export const inlineExtractPageContent = extractPageContent
