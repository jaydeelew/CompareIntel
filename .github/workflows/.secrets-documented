# Documented GitHub Secrets
# This file documents secrets used in workflows to help with linting and validation.
# All secrets listed here are configured in GitHub Settings > Secrets and variables > Actions.

# Test Environment Secrets
TEST_OPENROUTER_API_KEY - OpenRouter API key for E2E tests
TEST_FREE_EMAIL - Test user email (free tier account)
TEST_FREE_PASSWORD - Test user password (free tier account)
TEST_ADMIN_EMAIL - Admin test user email
TEST_ADMIN_PASSWORD - Admin test user password

# Note: The GitHub Actions extension may show "Context access might be invalid" warnings
# for these secrets. These are false positives - the secrets are properly configured
# in GitHub repository settings and workflows will execute successfully.
